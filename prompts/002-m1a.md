角色与使命:你是 uniterm KaihongOS 桌面版适配开发的执行工程师,上级编排者(ZCode)已审核你的设计书并批准路线。本次执行里程碑 M1a:工程落地 + 最大风险前置消解(libssh PoC)。

编排者决策(已拍板,直接执行,不要复议):
- 路线批准:HAP(ArkTS/ArkUI)壳 + @ohos/libssh(HAR, NAPI)进程内集成;回退路线 b'(libssh C 源自编译)仅在 PoC 证伪时启用,启用前必须先上报。
- D1 LGPL:本项目为内部适配、只安装到本机 VM、不对外分发,LGPL-2.1 直接采用。
- D2 bundleName = **net.uniterm**(不要用 com.khds.* 前缀);应用显示名 **uniTerm**。
- D3 M1 包含「测试连接」按钮。
- D4 加密 .utm 兼容移到 M3;M1 只做明文 connections.json 与明文 .utm 导入(导入功能属于 M1b,本次 M1a 可不做)。
- D5 界面文案仅 zh-CN。
- 勘误:今天是 **2026-09-20**,你设计书里写的 2026-10-24 有误;请把 ADAPTATION_DESIGN.md 的日期改正,后续所有文档用真实日期。

环境事实(已核实):
- 设备:hdc -t 127.0.0.1:5555,KaihongOS 5.0.2.57,x86_64,API 24,已开机。
- SDK:C:\Program Files\Huawei\DevEco Studio\sdk\default\openharmony(API 26);hvigorw/ohpm/hdc 均可用(路径见设计书⑥)。
- **SSH 靶机:宿主机 127.0.0.1:22 有 SSH 服务监听。QEMU 用户网络下,guest 访问 10.0.2.2:22 会经 SLIRP 网关转到宿主机回环 22 端口。** PoC 先只取 server banner(SSH-2.0-...),不需要认证;若连不上或 banner 异常,备选靶机:WSL 里起 sshd(注意 WSL2 端口转发问题)或 guest 内自建。
- 上游源码 D:\uniterm\upstream 只读参考,**禁止改动**。你的工作目录:D:\uniterm\kaihongos(新建)。

M1a 任务清单(按序执行,每步留痕):

1. 脚手架:在 D:\uniterm\kaihongos 创建标准 hvigor 工程(IDEA 结构:AppScope + entry 模块,ArkTS/ArkUI,bundleName=net.uniterm,compileSdkVersion=26 / compatibleSdkVersion=24 / targetSdkVersion=24,abiFilters 含 x86_64)。无 IDE 参与,纯命令行。
2. 依赖:ohpm install @ohos/libssh(预期 v1.0.4),记录 har 落位。若该库在 x86_64 下无预编译产物或装不上,如实记录失败模式。
3. 构建与签名脚本化:
   - scripts\build.cmd:ohpm install + hvigorw assembleHap,产出未签名 HAP。
   - scripts\sign.cmd:用 SDK toolchains 下 hap-sign-tool 生成/复用本地调试证书材料(优先复用 %USERPROFILE%\.ohos\config 既有 DevEco 自动签名材料,若无则脚本生成自签 CA+profile),产出 entry-default-signed.hap。全部密钥口令写入脚本注释,内部项目不设防。
   - scripts\deploy.cmd:hdc -t 127.0.0.1:5555 install + aa start。
4. 最小可装应用 + SSH PoC:
   - 首页(ArkUI):显示"uniTerm KaihongOS M1a"与构建号;一个「SSH PoC 连接 10.0.2.2:22」按钮;一个结果区。
   - 点击后经 @ohos/libssh 建立连接、取回 server banner,把 **SSH-2.0-…** 字符串直接渲染在页面上(不许只写 hilog);失败时页面显示错误码/错误串。
   - hilog 里同步打 tag=uniterm.poc 的结构化日志(开始/成功/失败+原因),供机检。
5. 装机验证:hdc 安装 → 启动 → 确认无 FATAL(冷启动 hilog 检查)→ 触发 PoC → 成功与失败两种路径都要试(失败路径可用一个不通端口 10.0.2.2:2222 触发,验证错误展示不 crash)。
6. 证据归档到 D:\uniterm\evidence\M1a\:
   - 截图 ≥3 张(首页、PoC 成功页、PoC 失败页)——截图手段自选:hdc shell snapshot_display / uitest dumpTree+截图 / hidumper,能看清即可;
   - hilog 导出(含 uniterm.poc 与 FATAL 检查结果)、构建日志、install 日志;
   - 写 D:\uniterm\evidence\M1a\REPORT.md:做了什么、结果、遗留问题。

红线:
- libssh 若在 x86_64/API24 编译不过/加载失败/连不上:**立即停止并上报失败模式与 b' 回退计划**,不要自行切换方案、不要降低验收标准。
- 不要为了通过而 hardcode banner 字符串——banner 必须来自真实网络读取,hilog 与页面双证据。
- 工程所有文件落在 D:\uniterm\kaihongos 与 D:\uniterm\evidence,不得污染其他目录。

回复要求:结束时输出 ≤30 行摘要(构建/签名/装机/PoC 四项各一行结论 + 证据文件相对路径 + 遗留问题)。
