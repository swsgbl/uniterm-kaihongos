角色与使命:uniterm KaihongOS M2 第 4 棒(收官棒):**终端渲染精化 + 性能基准**。棒 3b 五项验收已由编排者独立复核通过(截图+hilog 交叉验证)。断点事实源 D:\uniterm\evidence\M2\RESUME-STATE.md(含下一棒开工必读、证据链索引、WSL keepalive 注意事项)。纪律继承。

本棒任务:
1. **ANSI 解析升级**:最小可用解析器——CSI 序列分类处理:SGR 颜色(前景 8/16 色+256 色+默认复位,背景可只做默认/黑)、光标控制(\r \n \b、CUU/CUD/CUF/CUB、清行/清屏 EL/ED)、隐式回滚;不支持的序列安全吞掉(不许显示乱码)。参考上游 frontend/src/vendor 或 xterm.js 行为语义即可,不引入新三方库。
2. **光标渲染**:文本流模式下做"逻辑光标"(当前行内位置)可见即可;退格/方向键/清行行为正确。
3. **键盘直通(桌面形态关键)**:终端区域获得焦点后硬件键盘直接进 SSH stdin(方向键/Tab/Ctrl 组合尽量透传,Enter=\r);保留底部输入行作为 IME/复杂输入兜底(中文输入走输入行)。Ctrl 组合至少 Ctrl+C。
4. **滚动缓冲**:上限 5000 行裁剪,滚动流畅。
5. **性能基准**(设计书风险#3 的消解):
   - 远端 `seq 1 20000` 全量输出:记录总耗时与 hilog data chunks/bytes,目测无冻结(截图首尾);
   - `yes | head -c 2M` 或 `dd if=/dev/zero bs=1024 count=2048 | base64` 2MB 吞吐:记录 MB/s;≥0.5MB/s 判过线(设计书 2MB/s 目标若不达,记录差距+瓶颈分析,不阻塞 M2 验收,列入 M3 优化)。
6. 装机回归:老功能(连接管理/导入/PoC)不回归;五项验收抽两项复测(stty size 因渲染升级可能变值,以新量测为准)。

验收与归档(evidence\M2\):截图≥4(带色输出如 ls --color/hl、光标操作、20000 行前后、性能页)、hilog 性能计数、REPORT-relay4.md(M2 四棒总汇+性能数据)、RESUME-STATE.md 标注 **M2 完成**。

红线:键盘直通后输入必须仍走真实 SSH 数据流;性能数据必须来自实测(hilog 时间戳差),禁止估算;产物只落 kaihongos 与 evidence\M2。

回复要求:≤16 行:五项任务各一行结论、性能实测两行(20000 行耗时 / 2MB MB/s)、回归结果、遗留(给 M3)。
